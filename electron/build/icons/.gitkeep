# Place icon.ico (Windows), icon.icns (macOS) and 512x512 icon.png (Linux) here.
# electron-builder picks them up by filename via the `icon:` keys in
# electron-builder.yml. Empty until v1.x branding pass.
