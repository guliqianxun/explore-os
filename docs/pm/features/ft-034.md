---
ft_id: ft-034
title: services 接口规整 — LLM 中台层 + 三段中台跨段解耦
milestone: v1.2
sprint: iter-019
status: in_progress
priority: P0
created_at: 2026-04-29
depends_on: [phase-2-decision]
supersedes_draft: index.json 原草图
---

# ft-034 — services 接口规整（LLM 中台层 + 跨段解耦）

## 背景

需求堆叠（ft-027 → ft-028 → ft-029 → ft-033）落地速度优先于结构整洁。
4/29 6 组并行 review（A/B/C/D/E/F）共定位 ~90 个问题，PM 在 PHASE-2-DECISION
拍板把"必修 + 阻塞 ft-034"的 7 条（P0-1 ~ P0-7）合并为本 ft 的实际范围，
~6 工作日。

**底线**：本 ft 不引入新功能、不改 schema、不改用户可见行为；只重整代码边界。

## 不做（明示，防止滑坡）

- ❌ chat agent / function call / MCP server（v1.4+）
- ❌ user_profile 蒸馏（ft-035，v1.4+）
- ❌ FTS5 / Library 视图（ft-030，v1.3）
- ❌ APScheduler 改造（v1.5）
- ❌ 老 email pipeline `subscriptions/run_subscription.py` 弃用决策（v1.4）
- ❌ 全量测试覆盖率冲量
- ❌ 新依赖 / 升大版本

## 范围（7 条 P0）

### P0-1 LLM 中台层 `apps/llm/`（1 d）

新建 Django app `apps/llm/`，结构：

```
apps/llm/
  __init__.py
  apps.py                  # AppConfig
  client.py                # chat() / chat_json()，搬自 interpret/llm.py
  errors.py                # LLMError + 子类（Timeout / RateLimit / JsonParse）
  prompts/
    __init__.py            # registry: get_prompt(name, version)
    skim.py / deep.py / extract.py / brief.py / rewriter.py / figure_picker.py
  models.py                # ModelProfile registry：能力 → (model_name, max_tokens, temperature)
  services/
    __init__.py            # 5 LLM 能力 public 接口
    skim_interpret.py
    deep_interpret.py
    extract_claims.py      # L1+L2 claim 抽取（迁自 apps/interpret/interpreter.py）
    brief_generate.py
    rewriter.py            # 兴趣 → query
  tests_client.py / tests_prompts.py / tests_services.py
```

**legacy `interpret/llm.py` 保留 thin re-export wrapper 一周**（不强删，
避免老 email pipeline 在 P2-1 决策前断链）：

```python
# interpret/llm.py（一周观察期）
from apps.llm.client import chat, chat_json, extract_json
from apps.llm.errors import LLMError
__all__ = ["chat", "chat_json", "extract_json", "LLMError"]
```

### P0-2 prompt 注册表 + model registry + budget 中心（1 d）

- 7 处 SYSTEM 字符串（`tldr.py:14` `interpretation.py:54` `deep_interpret.py:26`
  `narrative.py:20` `rewriter.py:18` `figure_picker.py:91` `figure_classifier.py:22`）
  全数迁入 `apps/llm/prompts/<name>.py`
- 5 处 `settings.LLM_*` 直读（`deep_interpret.py:13` `figure_picker.py:19`
  `figure_classifier.py:11` `embedding.py:8` 等）→ `apps/llm/models.py`
  ModelProfile 集中读 settings
- 截断阈值散落 4 处 → `apps/llm/budgets.py`：
  - MARKDOWN_CHAR_BUDGET = 30_000
  - TLDR_MAX_LEN = 80
  - DEEP_CAPTION_LIMIT = 8
  - FIGURE_PICKER_TOP_K = 15
- max_tokens / temperature 默认值修正（512/0.3 改为合理值）

### P0-3 brief_generator 反向胶水切断（1.5 d）

- `apps/papers/brief_generator.py:191-192` 函数体内惰性 `from interpret.deep_interpret`
  / `from interpret.interpretation` → 改 import `apps.llm.services.{deep_interpret,
  skim_interpret}`
- `brief_generator.py:55,69-73,96-97,126` 5 次惰性 `from apps.extract.*` 全部
  迁入 `apps/llm/services/brief_generate.py`（material 装配是 brief 的内部职责）
- brief_generator 瘦身为 thin orchestrator：取 paper → 调 brief_generate 服务 → 持久化
- 复跑 ft-033 brief 视图，字段 + 视觉无回归

### P0-4 extract 暴露 public API（0.5 d）

- `apps/interpret/interpreter.py:43-55 get_paper_markdown` 直接调
  `apps.extract.extractors.docling_ext._convert`（私有下划线）
- 改为：`apps/extract/extractor.py` 暴露 `get_paper_markdown(paper_id) -> str`，
  内部封装 docling 调用 + caching
- `apps/interpret` + `apps/llm/services/*` 都改 import public API
- private `_convert` 加 module-level 注释禁止跨 app 调用

### P0-5 BASE_DIR 4 处漏网 → paths.* 化（0.5 d）

- `apps/extract/management/commands/extract_paper.py:32` BASE_DIR/'media'/'pdf' →
  `apps.core.paths.pdf_dir()`
- `apps/interpret/management/commands/interpret_paper.py:32` 同
- `apps/extract/figure_extractor.py:40` BASE_DIR/'media'/'figures' →
  `apps.core.paths.figures_dir()`
- `apps/render/management/commands/render_graph.py` 文案中残留 BASE_DIR
  字串 → 一并改
- 验收：`grep -r "BASE_DIR / 'media'" apps/` 0 处

### P0-6 DTO drift 修（0.5 d）

- 新建 `apps/api/serializers/jobs.py`：`JobSerializer`，status enum 5 值
  （pending/running/done/failed/cancelled）
- 新建 `apps/api/serializers/equations.py`：补 `paper_arxiv_id` / `eq_label` /
  `bbox` 字段
- frontend `types/paper-job.ts`（新文件）：JobStatus enum 同步 5 值
  - **过渡兼容**：后端在 1 个 sprint 内同时接受 `queued` 老值（mapping →
    `pending`），保留 deprecation log；v1.3 删除老值支持
- frontend `types/paper-material.ts`（新文件）：EquationDTO 4 字段补齐
- `types/paper.ts` 拆为 `paper-core.ts` / `paper-material.ts` / `paper-claim.ts`
  / `paper-job.ts`，老文件 re-export 保兼容

### P0-7 views.py 拆包（1 d）

`apps/api/views.py` 859 行 → `apps/api/views/` package：

```
apps/api/views/
  __init__.py              # re-export 保 urls.py 不变
  papers.py                # PaperListView / PaperDetailView / BriefRegenerate
  materials.py             # SectionView / FigureView / TableView / EquationView / CitationView
  claims.py                # ClaimListView / ClaimVerdictView
  jobs.py                  # JobListView / JobDetailView / JobCancel
  subscriptions.py         # SubscriptionList / Update
```

frontend 同步：`api/papers.ts` 352 行 → `api/papers/{core,material,claim,brief}.ts`
+ `api/jobs.ts` + `api/subscriptions.ts`，老 `papers.ts` re-export 保兼容（1
sprint deprecation 后删除）。

## 验收

| 项 | 标准 |
|---|---|
| 测试 | `pytest -q` ≥ 315 passed（基线不退）；新增至少 8 个 `apps/llm/` 测试 |
| 类型 | `tsc -b` 0 error |
| 构建 | `npm run build` 主 chunk 字节数不超基线 +50 KB |
| 调用图 | `grep "from interpret" apps/papers/brief_generator.py` 0 处 |
| 调用图 | `grep "from interpret.llm" apps/interpret/` 0 处（改为 `from apps.llm.client`） |
| 调用图 | `grep "_convert" apps/interpret/` 0 处 |
| 调用图 | `grep "BASE_DIR / 'media'" apps/` 0 处 |
| 视觉 | brief 视图 + Reading Station 用户实测无回归 |

## 派发分组（Phase 3）

| 组 | 内容 | 工作量 | 依赖 |
|---|---|---|---|
| F1 | P0-1 + P0-2 | 2 d | — |
| F2 | P0-3 + P0-4 | 2 d | F1 |
| F3 | P0-6 + P0-7 + P1-6（papers.ts 拆） | 1.5 d | — |
| F4 | P0-5 + P1-1~5 死码清理 | 1 d | — |

**顺序**：F1 → (F2 ∥ F3 ∥ F4) → 主会话顺序 merge + 全测。

## 顺手清单（P1，可放本 sprint 也可推 v1.3）

按 PHASE-2-DECISION §2 P1：6 个 0 字节 stub / 顶级 tldr+figure_classifier 死码 /
3 thin wrapper / 4 management commands / frontend claims.ts / papers.ts 拆。
本 ft 落 F4 时一起做。

## 风险

- **brief 视图 4/29 today 落地（ft-033）**：F2 改动可能踩到刚定型字段；用户实测必跑
- **legacy `interpret/llm.py` thin wrapper 留 1 周**：F1 不强删，避免老 email
  链断；P2-1 决策后再删
- **JobStatus 改名后端兼容**：1 sprint 过渡，期间日志监控 `queued` 出现频率
- **6 个 0 字节 stub 删除**：删前 grep `from .models` / `from .admin`

## 不变量（重构期不可破坏）

- `papers_user_*` 表 schema 不动（操作历史种子）
- `papers_brief` 表 schema 不动（ft-033 字段定型期）
- ft-029 Reading Station 视觉行为不变
- 老 email pipeline 入口 `python manage.py run_subscription` 仍可跑（暂留 thin
  wrapper 让它能 import 老 `interpret.*`）

## 后续

- v1.3 iter-020：ft-030 Library + FTS5（papers.ts 已拆好正好用）
- v1.4：chat 分级 + ft-035 user_profile 蒸馏（apps/llm/services 已 ready）
- v1.4 决议：P2-1 老 email 链是否弃
