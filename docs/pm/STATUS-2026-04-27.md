---
pm_id: status-2026-04-27
pm_type: snapshot
generated_at: 2026-04-27
---

# explore-os Status Snapshot · 2026-04-27

## 一句话现状

**三段中台 + Electron 桌面化主干闭环；ft-027 (subscription 表单化 + Ingest) 实测中；前端编辑风格已落地；ft-025 自动更新+打包待启动。**

## 里程碑进度

| Milestone | 内容 | 状态 |
|---|---|---|
| **v0.1 MVP (M1)** | arXiv + HF / TL;DR / 邮件 / CLI | ✅ done |
| **v0.2 (M2)** | rerank / 精读+略读分档 / Daily Narrative | ✅ done |
| **v0.3 (M3)** | PDF 拉取 + 图表 + 多模态 | ✅ done（ft-013 重设计后稳） |
| **v0.4 (M3.5)** | caption + bbox + 记忆线 + 双图 | ✅ done |
| **v0.5 (M4)** | DeliveryAdapter 抽象 ✅；飞书/微信 deferred | partial（多渠道按需） |
| **v0.6 (M5)** | 抽取层 (5 类 material) + Docling 替换启发式 | ✅ done |
| **v0.7 (M6)** | 解读器 L1+L2 (claim → evidence + 反向信号) | ✅ done |
| **v0.8 (M7)** | 图谱抽象层 + Excalidraw + SVG（freeze，可视化推 v1.x） | ✅ done frozen |
| **v0.9 (M8)** | Electron 基础设施 + DRF API + EXPLORE_OS_DATA_DIR | ✅ done |
| **v1.0 (M9)** | Electron shell + Python sidecar + PyInstaller spec | ✅ done（沙箱不验真打包） |
| **v1.1 (M10)** | 前端 MVP 4 页 → 编辑/杂志重设计 → subscription/ingest | 🔄 ft-024 ✅ / ft-026 ✅ / **ft-027 in_progress** |
| **v1.2 (M11)** | 自动更新 + CUDA/CPU 双轨打包 | 📋 planned (iter-012) |

## Features 状态汇总

**Done (24 个)**：ft-001 ~ ft-014（v0.1–v0.4 全套）/ ft-015 / ft-016 / ft-019 / ft-020 / ft-021 / ft-022 / ft-023 / ft-024 / ft-026

**In progress (1 个)**：
- **ft-027** SubscriptionPage 表单化 + RunPage → Ingest（PDF / arXiv / URL）
  - 代码 + 测试 ✅ commit (`7fc7075`)
  - Run now 异步反馈 follow-up ✅ commit (`ffc27a7`)
  - HF_HOME 复用全局缓存 fix ✅ commit (`d067321`)
  - 用户实测中（最后一次窗口关闭，需确认 ingest 是否能完整跑通三阶段）

**Planned (1 个)**：
- ft-025 自动更新 + CUDA/CPU 双轨（v1.2，iter-012）

**Deferred (3 个)**：
- ft-012（已被 ft-013 superseded）
- ft-017 飞书 / ft-018 微信订阅号（按需推进，非主线）

## Sprint / Iteration

| Iter | 范围 | 状态 |
|---|---|---|
| iter-001 ~ iter-003 | MVP / 注意力分层 / 精读深度化 | ✅ done |
| iter-004 | ft-019 抽取层 + ft-022 packaging 调研 + ft-015 Docling | ✅ done |
| iter-005 | ft-020 解读器 | ✅ done |
| iter-006 | ft-021 渲染层（cluster cards + Excalidraw） | ✅ done frozen |
| iter-007 | ft-022 Electron 基础设施 + DRF API | ✅ done |
| iter-008 | ft-023 Electron shell + sidecar | ✅ done |
| iter-009 | ft-024 前端 MVP 4 页 | ✅ done |
| iter-010 | ft-026 编辑/杂志重设计 + CORS 修复 | ✅ done |
| **iter-011** | **ft-027 subscription 表单化 + Ingest** | 🔄 **in_progress** |
| iter-012 | ft-025 自动更新 + CUDA/CPU（待启动） | 📋 planned |

## 战略锁定（已成 CLAUDE.md / ROADMAP 决策）

- **桌面端**：直上 Electron（Tauri 弃），unsigned 自用，CUDA bundle 自用 + CPU bundle 留分发接口
- **前端栈**：Vite + React 18 + TS strict + Tailwind 3 + shadcn/ui + KaTeX + Zustand + TanStack Query
- **设计调性**：Editorial / Magazine（Distill × Stratechery），衬线正文 + 暖中性 + deep evergreen accent
- **图谱可视化推迟到 v1.x**：v0.8 cluster cards 实测可读性不及 HTML，索引层数据已建立是后续视图基础
- **数据层**：避免 PG-only，全 SQLite 友好；同库前缀 `extract_*` / `interpret_*` / `render_*` / `interpret_v2_*`
- **路径**：`apps/core/paths.py` 单源 of truth；`EXPLORE_OS_DATA_DIR` env > 平台默认；HF_HOME 复用用户全局 `~/.cache/huggingface`
- **存储**：subscription 仍走 YAML（ruamel.yaml 保留注释，不切 DB）

## 测试 / 构建基线

- **Python pytest**: 260 passed
- **frontend npm run build**: 0 TS error, dist 801KB / gzip 249KB
- **electron tsc**: 0 error
- **沙箱本机实测**：Vite + Electron + sidecar 三进程握手通过；GET /api/papers/ 返回 leworldmodel/convnextv2 数据

## 已知 follow-ups（不阻塞）

1. **CLI 默认 PDF 路径**：`extract_paper`/`interpret_paper` CLI 默认仍读 `BASE_DIR/media/pdf`，未在 ft-022 6 个替换点中。`paths.pdf_legacy_dir()` 已就绪，后续 ft 一行 patch
2. **Subscriptions API 在 ft-024 中暴露占位**：ft-027 已补完整 CRUD/run，frontend SubscriptionPage 已切实数据
3. **bundle 798KB**：vite 警告 chunk > 500KB，未做 code-splitting，留 v1.x 优化
4. **app.setName('explore-os')** 未生效：Electron sidecar 实际 DATA_DIR 仍是 `%APPDATA%/Electron`（不是 explore-os），但 HF_HOME 复用用户全局缓存后此问题不再阻塞 ingest
5. **图谱可视化**：cluster cards + Excalidraw + SVG 已 freeze，等 v1.x Electron 内做交互式

## Backlog（v1.x+）

- ft-025 自动更新 + CUDA/CPU 双轨（next，iter-012）
- 跨篇图谱 / Daily Narrative 网状关联
- 飞书 / 微信订阅号 DeliveryAdapter（按需）
- xingsuo 接入作为 source
- 公开分发签名（Apple Dev / Win EV cert）
- legacy `interpret/*` 消费者迁移到读 L1+L2 表
- markdown 截断阈值 30K → 60-80K（DashScope deepseek-v4-flash 上下文允许）
- subscription 切 DB（v1.x，可选）

## 最近 10 commits

```
d067321 fix(core): HF_HOME 复用全局缓存避免 sidecar 重新下载
ffc27a7 feat(frontend): SubscriptionPage Run now 异步反馈
7fc7075 feat(api+frontend): ft-027 Subscription 表单化 + Ingest
cd1c0d0 docs(pm): 关 iter-010 + 开 iter-011 ft-027 派发
ec90145 feat(frontend): ft-026 editorial/magazine 重设计 + CORS
c50bf5f docs(pm): 开 iter-010 + dsp-009 ft-026 派发
232fec5 docs(pm): rpt-008 + iter-009 关闭（ft-024 ✅）
9459b16 chore(frontend): gitignore tsc build 产物
3655fcb feat(frontend+ft-023 followups): ft-024 MVP 4 pages
72ac210 docs(pm): 开 iter-009 + dsp-008 ft-024 派发
```

## 仓库结构（决策中台）

```
apps/
├── core/         paths.py + scheduler.py（EXPLORE_OS_DATA_DIR + APScheduler）
├── api/          DRF 9+8 endpoints（papers / claims / triggers / jobs / subs / ingest）
├── extract/      DoclingExtractor + 5 material models
├── interpret/    Claim / ClaimEvidence / CounterSignal models + L1+L2 interpreter
└── render/       PaperGraphModel + Excalidraw/SVG renderers + LaTeX matplotlib

electron/         Electron shell（main/sidecar/preload/port.ts + builder.yml）
frontend/         Vite+React+TS+Tailwind+shadcn/ui+KaTeX SPA（4 页 + Editorial 风格）
build/            sidecar-cuda.spec PyInstaller + hooks/hook-torch.py
sidecar_entry.py  Waitress 启 Django sidecar
```

## 下一步候选（按优先级）

1. **完成 ft-027 实测**：用户确认 ingest 三阶段（extract → interpret → render）跑通
2. **iter-012 / ft-025**：自动更新（electron-updater）+ CUDA/CPU 双轨 PyInstaller spec
3. **微 polish backlog**：bundle code-splitting / app.setName 真生效 / CLI 默认路径走 paths.pdf_legacy_dir
