---
pm_id: rpt-001
pm_type: report
workstream: backend-extract
feature: ft-019
dispatch_ref: dsp-001
status: completed
created_at: 2026-04-25
---

# rpt-001：ft-019 抽取器素材索引层 完成

## 交付
- `apps/extract/` Django app（5 model + Extractor Protocol + 6 dataclass + DefaultExtractor + persist_result + extract_paper CLI）
- 三个搬迁模块：section_extractor / caption_extractor / figure_extractor
- 两个新写模块：equation_extractor（启发式）/ citation_extractor（启发式）
- 28 个新单元测试 + 既有测试 import 改 wrapper

## 验证
- `makemigrations extract` → 0001_initial 生成，全 JSONField 无 PG-only
- `apps/extract/` 28 passed
- 全量回归 145 passed（PG 缺失导致 1 error，非代码问题；agent 沙箱 sqlite 下 133 全通）
- ruff 通过

## 合并
- worktree branch `worktree-agent-aeadcfc7` → main（merge commit `4557483`）
- 冲突 1 处（`interpret/caption_extractor.py`）：取 wrapper；同时把 `apps/extract/caption_extractor.py` 替换为 `dec8d7c` 版（保留 b73d4b8 的 bbox 多策略 + FIGURE_CLUSTER_TOL 修复）

## 偏差
- 未真实跑 `extract_paper 2401.12345`（agent 沙箱无缓存 PDF + 无 PG）。CLI 已写就绪，本地有缓存时可验证

## 后续
- ft-020 可启动（解读器 L1+L2 直接读 `extract_*` 表）
- 旧 `interpret/{pdf_chunker,caption_extractor,figure_extractor}.py` thin wrapper 留作过渡，ft-020 阶段统一清理
