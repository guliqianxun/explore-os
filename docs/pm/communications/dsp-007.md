---
pm_id: dsp-007
pm_type: dispatch
target: frontend-shell
action: develop
feature: ft-023
priority: P0
status: pending
created_at: 2026-04-26
deadline: 2026-05-03
---

# 任务: Electron shell + Python sidecar + PyInstaller（ft-023）

## 背景

iter-007 ft-022 已落地 DRF API + EXPLORE_OS_DATA_DIR + APScheduler。
本任务搭 Electron 壳：主进程 + Python sidecar (waitress) + HTTP localhost 通信。
**不含前端 UI**——空 BrowserWindow 即可（前端留 ft-024）。

完整 spec 见 [features/ft-023.md](../features/ft-023.md)。

**关键说明**：直接在主仓 `E:\codes\explore-os\` 工作（不用 worktree，避免 base 漂移）。沙箱不会有 npm / electron-builder / PyInstaller 工具链——你只产**文件骨架**，最终构建验证由用户本机做。

## 严格边界

**仅允许新建/修改**：
- 新建 `electron/` 整目录（package.json + tsconfig + src/ + 配置）
- 新建 `sidecar_entry.py`（项目根）
- 新建 `build/` 目录（PyInstaller spec + electron-builder yml）
- 修改 `pyproject.toml`（dev-dep 加 pyinstaller + waitress）
- 修改 `.gitignore`（排除 electron/dist + node_modules + build/dist）

**严禁触碰**：
- `apps/{extract,interpret,render,core,api}/**`（已 frozen）
- `interpret/**` legacy
- `delivery/**`、`sources/**`、`subscriptions/**`、`config/**`
- `frontend/**`（不存在，不要建，留 ft-024）
- `docs/**`
- 严禁 git commit

## 文件白名单

**新增**（按目录分组）：

```
electron/
├── package.json
├── tsconfig.json
├── .gitignore（local）
├── README.md（一页 dev/build 说明）
├── src/
│   ├── main.ts          # Electron main process
│   ├── preload.ts       # contextBridge expose API
│   ├── sidecar.ts       # Python sidecar 启动 / 监控 / 清理
│   ├── port.ts          # 端口管理
│   └── types.ts         # 共享类型
└── build/
    ├── electron-builder.yml
    └── icons/.gitkeep   # 图标占位

sidecar_entry.py         # 项目根，Waitress 跑 Django
build/
├── sidecar-cuda.spec    # PyInstaller spec
└── hooks/
    └── hook-torch.py    # torch CUDA hidden import collect
```

**修改**：
- `pyproject.toml`：`[dependency-groups] dev` 追加 `"pyinstaller>=6.10"` + `"waitress>=3.0"`
- 项目根 `.gitignore`：追加 `electron/dist`、`electron/dist-electron`、`electron/node_modules`、`build/dist`、`build/__pycache__`、`*.spec.bak`

## 接口契约

### sidecar_entry.py
```python
"""PyInstaller / dev entry: starts Django via Waitress on given port + DATA_DIR."""
import argparse, os, sys

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--port", type=int, default=0,
                        help="0 = OS picks free port; prints chosen port to stdout")
    parser.add_argument("--data-dir", required=True)
    parser.add_argument("--host", default="127.0.0.1")
    args = parser.parse_args()
    os.environ["EXPLORE_OS_DATA_DIR"] = args.data_dir
    os.environ.setdefault("DJANGO_SETTINGS_MODULE", "config.settings")
    import django
    django.setup()
    from django.core.management import call_command
    call_command("migrate", "--noinput")
    from waitress import create_server
    from config.wsgi import application
    server = create_server(application, host=args.host, port=args.port)
    actual_port = server.sockets[0].getsockname()[1]
    print(f"[sidecar] listening on http://{args.host}:{actual_port}", flush=True)
    sys.stdout.flush()
    server.run()

if __name__ == "__main__":
    main()
```

### electron/src/main.ts 骨架
```typescript
import { app, BrowserWindow } from "electron";
import * as path from "path";
import { startSidecar, stopSidecar } from "./sidecar";

let mainWindow: BrowserWindow | null = null;
let sidecarPort: number | null = null;

async function createWindow() {
  sidecarPort = await startSidecar();
  mainWindow = new BrowserWindow({
    width: 1280, height: 800,
    webPreferences: {
      preload: path.join(__dirname, "preload.js"),
      contextIsolation: true,
      nodeIntegration: false,
    },
  });
  // dev：Vite dev server URL 由 env 注入；prod：加载本地占位 HTML（ft-024 之前）
  if (process.env.VITE_DEV_SERVER_URL) {
    mainWindow.loadURL(process.env.VITE_DEV_SERVER_URL);
  } else {
    mainWindow.loadFile(path.join(__dirname, "../resources/placeholder.html"));
  }
}

app.whenReady().then(createWindow);
app.on("window-all-closed", () => {
  stopSidecar();
  if (process.platform !== "darwin") app.quit();
});
app.on("before-quit", stopSidecar);
```

### electron/src/sidecar.ts 骨架
```typescript
import { spawn, ChildProcess } from "child_process";
import { app } from "electron";
import * as path from "path";

let proc: ChildProcess | null = null;

export async function startSidecar(): Promise<number> {
  const dataDir = app.getPath("userData");
  const isDev = !!process.env.VITE_DEV_SERVER_URL;
  let cmd: string, args: string[];
  if (isDev) {
    // dev: 用项目根 uv run python sidecar_entry.py
    cmd = "uv";
    args = ["run", "python", "sidecar_entry.py", "--data-dir", dataDir, "--port", "0"];
  } else {
    // prod: PyInstaller bundle
    const exe = process.platform === "win32" ? "explore-os-sidecar.exe" : "explore-os-sidecar";
    cmd = path.join(process.resourcesPath, "sidecar", exe);
    args = ["--data-dir", dataDir, "--port", "0"];
  }
  proc = spawn(cmd, args, {
    cwd: isDev ? path.join(__dirname, "../..") : undefined,
    env: { ...process.env, PYTHONUNBUFFERED: "1" },
    stdio: ["ignore", "pipe", "pipe"],
    shell: process.platform === "win32",  // uv on Windows 需要 shell
  });
  proc.stdout?.on("data", (b) => console.log("[sidecar]", b.toString().trim()));
  proc.stderr?.on("data", (b) => console.error("[sidecar]", b.toString().trim()));
  // 解析 stdout 第一行 "listening on http://..." 拿端口
  const port = await waitForPort(proc, 30_000);
  await waitForHealth(port, 30_000);
  return port;
}

export function stopSidecar() {
  if (!proc || proc.killed) return;
  if (process.platform === "win32" && proc.pid) {
    spawn("taskkill", ["/F", "/T", "/PID", String(proc.pid)]);
  } else {
    proc.kill("SIGTERM");
    setTimeout(() => proc?.kill("SIGKILL"), 5_000);
  }
  proc = null;
}

async function waitForPort(p: ChildProcess, timeoutMs: number): Promise<number> { /* ... 解析 stdout */ }
async function waitForHealth(port: number, timeoutMs: number): Promise<void> { /* ... 轮询 /api/health */ }
```

### electron/src/preload.ts
```typescript
import { contextBridge, ipcRenderer } from "electron";

contextBridge.exposeInMainWorld("explore", {
  getBackendPort: () => ipcRenderer.invoke("get-backend-port"),
});
```

main.ts 配套 `ipcMain.handle("get-backend-port", () => sidecarPort)`。

### electron/package.json 关键脚本
```json
{
  "name": "explore-os-shell",
  "version": "0.1.0",
  "main": "dist-electron/main.js",
  "scripts": {
    "tsc": "tsc -p tsconfig.json",
    "dev:electron": "npm run tsc && electron dist-electron/main.js",
    "dev:sidecar": "cd .. && uv run python sidecar_entry.py --data-dir ../data-dev --port 8000",
    "build:tsc": "tsc -p tsconfig.json",
    "build:sidecar": "cd .. && uv run pyinstaller build/sidecar-cuda.spec --noconfirm --distpath dist",
    "build:electron": "npm run build:tsc && electron-builder --config build/electron-builder.yml",
    "build:all": "npm run build:sidecar && npm run build:electron"
  },
  "devDependencies": {
    "electron": "^32.0.0",
    "electron-builder": "^24.13.0",
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
```

注：版本号写当前主流稳定，用户本机 `npm install` 时会按 semver 拉。

### build/sidecar-cuda.spec
```python
# -*- mode: python -*-
"""ft-023: PyInstaller spec for CUDA-flavored sidecar bundle (self-use)."""
from pathlib import Path
from PyInstaller.utils.hooks import collect_all

block_cipher = None
ROOT = Path.cwd()

# 收集复杂依赖（torch / docling / matplotlib / transformers）
torch_data, torch_binaries, torch_hidden = collect_all("torch")
docling_data, docling_binaries, docling_hidden = collect_all("docling")
docling_core_data, docling_core_binaries, docling_core_hidden = collect_all("docling_core")
mpl_data, mpl_binaries, mpl_hidden = collect_all("matplotlib")
trans_data, trans_binaries, trans_hidden = collect_all("transformers")

a = Analysis(
    ["sidecar_entry.py"],
    pathex=[str(ROOT)],
    binaries=torch_binaries + docling_binaries + docling_core_binaries + mpl_binaries + trans_binaries,
    datas=[
        ("config", "config"),
        ("apps", "apps"),
        ("interpret", "interpret"),
        ("delivery", "delivery"),
        ("sources", "sources"),
        ("subscriptions", "subscriptions"),
        *torch_data, *docling_data, *docling_core_data, *mpl_data, *trans_data,
    ],
    hiddenimports=[
        "django.template.loaders.app_directories",
        "rest_framework",
        "apps.core.apps", "apps.api.apps",
        "apps.extract.apps", "apps.interpret.apps", "apps.render.apps",
        "subscriptions.apps", "sources.apps", "interpret.apps", "delivery.apps",
        "waitress",
        *torch_hidden, *docling_hidden, *docling_core_hidden, *mpl_hidden, *trans_hidden,
    ],
    hookspath=["build/hooks"],
    runtime_hooks=[],
    excludes=["pytest", "ruff", "tkinter"],
    cipher=block_cipher,
    noarchive=False,
)
pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)
exe = EXE(pyz, a.scripts, [], exclude_binaries=True,
          name="explore-os-sidecar", debug=False, bootloader_ignore_signals=False,
          strip=False, upx=False, console=True)
coll = COLLECT(exe, a.binaries, a.zipfiles, a.datas, strip=False, upx=False,
               name="explore-os-sidecar")
```

### build/electron-builder.yml
```yaml
appId: dev.explore-os.app
productName: explore-os
directories:
  output: dist
  buildResources: build
files:
  - electron/dist-electron/**/*
  - electron/resources/**/*
  - electron/package.json
extraResources:
  - from: dist/explore-os-sidecar
    to: sidecar
mac:
  target: dmg
  identity: null
  category: public.app-category.productivity
win:
  target: nsis
  signingHashAlgorithms: null
linux:
  target: AppImage
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
```

## 沙箱限制（明示）
- 不安装 npm / Node.js（用户本机做）
- 不跑 `electron` / `electron-builder` / `pyinstaller`（用户本机验证）
- 不跑 `tsc` 编译（agent 只产 .ts 源，user 本机 npm install 后编译）
- 你**只产文件骨架**，把每个文件写完整 + 写 README 说明用户怎么跑

## 验收（你能做的）
1. 文件结构符合白名单
2. `pyproject.toml` 加 dev-dep（不破现有 sync）：`uv sync` 不挂
3. `sidecar_entry.py` 单跑能起 waitress（`uv run python sidecar_entry.py --data-dir <tmp> --port 8765` 应打印 `listening on ...` 然后阻塞等连接）
4. `python -c "import sidecar_entry; print('ok')"` 能 import 不报错
5. 既有全量 pytest 不破（241 全过）
6. 写一份 `electron/README.md` 说明：
   - 用户首次：`cd electron && npm install`
   - dev：项目根 `uv sync`，然后 `cd electron && npm run dev:electron`
   - build CUDA bundle：`npm run build:all`（需要 Windows + uv + Node 环境）

## 不要做
- 不要试图跑 npm install（沙箱无 Node）
- 不要试图跑 pyinstaller（耗时太长 + 输出 1-2GB）
- 不要建 frontend 目录
- 不要碰 apps/* 业务代码
- 不要 git commit

## 提交协议
你**没有 git commit 权限**。结束时报告：
- 改动清单
- `sidecar_entry.py` 启动测试结果（`uv run python sidecar_entry.py --data-dir <tmp> --port 8765` 看是否打印 listening）
- 全量 pytest 结果
- 主会话代为 commit + user 本机验证 npm/PyInstaller

## 参考
- Feature spec: [features/ft-023.md](../features/ft-023.md)
- Iteration: [iterations/iter-008.md](../iterations/iter-008.md)
- 项目约束: `/CLAUDE.md`

开始干活。