---
pm_id: rpt-008
pm_type: report
workstream: frontend-mvp
feature: ft-024
dispatch_ref: dsp-008
status: completed
created_at: 2026-04-26
---

# rpt-008：ft-024 前端 MVP 4 页 完成

## 交付
- `frontend/` 整目录（38 个文件）：Vite + React 18 + TS strict + Tailwind 3 + shadcn/ui + KaTeX
- 4 页：PaperListPage / PaperDetailPage / SubscriptionPage / RunPage
- 核心组件：ClaimCard（react-katex BlockMath）/ MarkdownView（react-markdown + rehype-katex）/ SectionTree / PaperCard / EquationBlock / CounterSignalBadge
- shadcn/ui 8 组件本地化（button / card / badge / dialog / scroll-area / tabs / textarea / input）
- ft-023 两个 follow-up:
  - electron/src/main.ts: `app.setName("explore-os")` + dev/prod loadURL
  - electron/build/electron-builder.yml: `files` 加 frontend/dist/**
  - sidecar_entry.py: 显式 override DATABASE_URL 覆盖 .env 优先级

## 验证（主会话代跑）
- `cd frontend && npm install` 成功（137 包）
- `npm run build`（vite + tsc strict）**0 error**，dist 778KB / gzip 244KB
- `cd electron && npx tsc` 0 error
- 全量 `pytest` 241 passed（零回归）
- 清理了 tsc 编译溢出的配置文件（tailwind.config.js / vite.config.js / *.tsbuildinfo），并补全 gitignore

## 偏离 / 已知点
1. **agent 沙箱被拒所有 shell**，无法本机 verify；主会话代为跑 npm install + build + pytest 全过
2. **subscriptions API endpoint 未暴露**：SubscriptionPage 优雅降级显示 "No subscriptions yet"
3. **PaperDetailPage 单向联动**：claim → markdown scroll；双向（graph→claim 选中）留 v1.x
4. **ClaimCard equation 区分**：从 evidences 按 material_id 前缀（`:eq:` / `:equation:`）切分，equationsById 反查 LaTeX 源码
5. **bundle 778KB**：vite 警告 chunk > 500KB，未做 code-splitting，留作 v1.x 优化

## 用户本机最终验证
1. terminal 1: `cd frontend && npm run dev`（Vite 起 5173）
2. terminal 2: `cd electron && set VITE_DEV_SERVER_URL=http://127.0.0.1:5173 && npm run dev:electron`
3. 走 4 页：列表 → 精读（ClaimCard KaTeX 真渲染）→ 订阅 → 抓取触发

## 后续
- iter-009 关闭
- iter-010 准备 ft-025（自动更新 + CUDA/CPU 双轨打包）
- 已知 backlog：bundle code-splitting / subscriptions API / PaperDetail 双向联动
