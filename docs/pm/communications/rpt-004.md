---
pm_id: rpt-004
pm_type: report
workstream: backend-interpret
feature: ft-020
dispatch_ref: dsp-004
status: completed
created_at: 2026-04-26
---

# rpt-004：ft-020 解读器 L1+L2 生产侧 完成

## 交付
- `apps/interpret/` 完整 Django app（label=interpret_v2）
- 3 张表：`interpret_claims` / `interpret_claim_evidence` / `interpret_counter_signals`
- L1 claim 抽取 + 强制 cite + confidence 阈值
- L2 论文内反向信号扫描（限定 limitation/ablation/failure_case/prior_work_better）
- `interpret_paper` CLI

## 验证
- `pytest apps/interpret/`: 33 passed
- 全量回归: 184 passed (151 → 184)
- ruff 通过
- 单测全 mock LLM + mock docling
- **真实 LLM 实战两篇全通过**：
  - LeWorldModel: 10 claims / 15 evidence / 5 signals（含 prior_work_better 类型识别 DINOv2 1.24 亿张图先验）
  - ConvNeXt V2: 9 claims / 11 evidence / 5 signals（含 83.7% vs 83.8% 微差对比 + GRN 必须前训+微调都加的 ablation_drop）
  - 1 个 ConvNeXt V2 claim 被强制 cite 拒绝（无 material_id），同信息被 L2 兜底抓回为 ablation_drop —— 机制设计正确

## 实战亮点（精读级别 nuance 全捕获）
- **claim cite 多 material 能力**：ConvNeXt V2 claim 8 cite 3 个 equations（GRN 公式）
- **claim 关键数字保留**：48× 规划速度 / 88.9% top-1 / 18% 提升等具体数字 LLM 能从论文正文抽出
- **counter_signal 类型识别准**：DINO-WM 用 DINOv2 预训练优势识别为 `prior_work_better` 而非 `failure_case`
- **强制 cite 机制有效**：ConvNeXt V2 一条无 cite claim 被丢，但同一信息 L2 兜底抓回（双层保险）

## 已知 tunable
- **Markdown 30K 截断**：LeWorldModel 全文 90K → 截到 30K（前 1/3）；ConvNeXt V2 50K → 截到 30K（前 60%）
  - 即便如此，关键 ablation/failure 都被 L2 抓到（catalog 提供了完整 table caption + caption 是高密度信息）
  - 阈值偏保守。DashScope `deepseek-v4-flash` 上下文允许更大；建议下次 tune 到 60–80K（一行 const 改）。
  - 不阻塞，留 ft-024 / ft-020.1 处理

## 偏离 / 决策记录
- 无重大偏离 dispatch
- 4 个测试模块 33 个 case，覆盖：
  - catalog 构造 + allowed 集合
  - 强制 cite 校验（合法 / 非法 / 缺失）
  - confidence 阈值丢弃
  - L2 claim_idx 越界 / evidence 不在 allowed 丢弃
  - L1/L2 LLMError 兜底（L1 错返空、L2 错保 L1）
  - persist 幂等性 + 孤儿 claim 清理

## 后续
- iter-005 关闭
- iter-006 准备 ft-021（渲染分级 + drawio 单篇图谱），可直接读 `interpret_*` 表
- 后置 ft-024：legacy `interpret/deep_interpret.py` / `figure_picker.py` / `run_subscription` 迁移到读 L1+L2
- 后置 micro-improvement：markdown 截断阈值 30K → 60–80K
