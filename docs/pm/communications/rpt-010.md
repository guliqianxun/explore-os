---
pm_id: rpt-010
pm_type: report
workstream: full-stack
feature: ft-027
dispatch_ref: dsp-010
status: completed
created_at: 2026-04-28
---

# rpt-010：ft-027 Subscription 表单化 + Ingest 完成（代码层）

## 交付（已 commit）

| Commit | 内容 |
|---|---|
| `7fc7075` | Backend：5 sub CRUD/run + 3 ingest endpoints + chain extract→interpret→render；Frontend：SubscriptionPage 卡片+modal+ChipInput / IngestPage 三入口 + IngestProgressItem |
| `ffc27a7` | SubscriptionPage Run now 异步反馈（job_id + 轮询） |
| `d067321` | HF_HOME 优先复用用户全局缓存 `~/.cache/huggingface`，避免 sidecar 首次拉 600MB docling 模型 |

## 验证

- `npm run build` 0 TS error
- `pytest -q` 241+ 通过（含 8 endpoint happy-path + chain 编排测）
- `ruamel.yaml` 已加，YAML 写回保留注释
- 主会话代为 commit（agent 无 git write 权限）

## 用户实测进展（4/27–4/28）

进 ingest 阶段触发 docling 首次下载 600MB 模型，沙箱内 `EXPLORE_OS_DATA_DIR/hf-cache/`
被覆写成空目录（非用户全局 ~/.cache/huggingface），导致重复下载。`d067321`
fix HF_HOME 优先级后未再观测，但**端到端三阶段链路在用户机器跑通**未拍照确认。

## 派生：ft-026 实测 follow-ups（同期主会话直接修，非 dsp-010 范围）

用户实测速读 detail 页时发现两个独立问题，主会话当场 fix（4/28 commit）：

| Commit | 内容 |
|---|---|
| `daaf73f` | docling `_map_sections` 累积 raw_text + similarity matcher 纳 path → figure 不再全沉底 |
| `c73a71d` | Electron 原生 menu/title 与 React header 双层重叠 → frameless + titleBarOverlay 40px + WebkitAppRegion drag |
| `b8842a7` | **PaperDetail 形态决策级 pivot**：markdown 重渲全文 → 速读卡片；删 4 个组件（MarkdownView / FloatingTOC / ClaimDrawer / ReadingModeToggle） |

最后一笔是产品决策级形态变更，单独立 ft-028 记账（Paper-centric schema +
user_* 层 + Inbox verdict 都依赖速读卡片这个新 detail 形态）。

## 收尾

- iter-011 待用户实测确认 ingest 三阶段链路即可关闭
- 主会话已写 ft-028 spec + 开 iter-012 准备地基级 schema 重构
