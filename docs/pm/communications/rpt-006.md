---
pm_id: rpt-006
pm_type: report
workstream: backend-core
feature: ft-022
dispatch_ref: dsp-006
status: completed
created_at: 2026-04-26
---

# rpt-006：ft-022 Electron 基础设施 + DRF API 完成

## 交付
- `apps/core/`：paths（统一持久化路径 + HF_HOME 重定向）+ scheduler（APScheduler 单例）
- `apps/api/`：DRF 9 endpoint 包装现有 CLI（health / papers / claims / triggers / jobs）+ in-memory job queue
- 6 处硬编码路径替换（subscriptions / sources / extract / render / delivery）
- SMTP fallback 写 `.eml` 到 outbox

## 验证
- `pytest apps/core apps/api`: 27 passed（paths 5 + scheduler 4 + jobs 4 + views 14）
- 全量回归: 241 passed (214 → 241)
- ruff `apps/core apps/api` 全过
- DATA_DIR 切换实测：所有路径函数指向 EXPLORE_OS_DATA_DIR；HF_HOME 自动设置；scheduler 可创建（待 sidecar 启时调 start_scheduler）

## 偏离 / 已知 follow-up
1. **CLI 默认 PDF 路径未改**：`extract_paper` / `interpret_paper` CLI 默认仍读 `BASE_DIR/media/pdf/<arxiv_id>.pdf`（这两个文件未在 dsp-006 6 个替换点列表内）。EXPLORE_OS_DATA_DIR 切换场景下需要显式 `--pdf-path` 参数。`paths.pdf_legacy_dir()` 已就绪，后续 ft 可一行 patch（不阻塞 ft-023）
2. **未实机跑 dev server / curl health**：沙箱命令受限，APIClient 单测覆盖等价场景
3. **worktree base 漂移问题**：第一次派发的 worktree 基线在 `33be9ac`（MVP 时期），缺所有 apps/*；改为不带 `isolation` 直接主仓写，跑通

## 后续
- iter-007 关闭
- iter-008 准备 ft-023（Electron shell + Python sidecar + PyInstaller）
- 已知 follow-up：CLI 默认 PDF 路径迁移到 paths.pdf_legacy_dir()
