---
pm_id: dsp-014
pm_type: dispatch
target: frontend
action: develop
feature: ft-029
priority: P0
status: pending
created_at: 2026-04-29
deadline: 2026-05-07
---

# 任务: ft-029 Frontend — Reading Station 全套（3 栏 + PdfViewer + ClaimCard 三态 + NotesPane）

## 背景

iter-013 Sprint 13。Backend dsp-013 已落地（rpt-013 in `docs/pm/communications/`），DTO 实测样例 + endpoint 已确认。本派单是 frontend 主切片（约 6–7 天，**允许分阶段提交，做不完写报告记账**）。

完整 spec：[features/ft-029.md](../features/ft-029.md)
迭代上下文：[iterations/iter-013.md](../iterations/iter-013.md)
Backend 报告：[rpt-013.md](rpt-013.md)（含 DTO 实测样例 + endpoint 契约）

## 关键约束

- **直接主仓写**（不用 worktree，参考 CLAUDE.md 第 9 条）
- **禁 git commit**（主会话代为）
- **禁起 dev server / 浏览器 / Electron**；可跑 `npm run typecheck` / `npm run build` / `npm run test`（如有）
- 路径：`E:\codes\explore-os\frontend\`
- TypeScript **strict** 必须 0 error
- 主 chunk 体量不能再涨超过 +50KB（pdf.js 走懒加载 split chunk）

## 文件白名单

```
frontend/src/pages/PaperDetailPage.tsx                # 改壳分发 station/speed
frontend/src/pages/SpeedReadView.tsx                  # 新建（既有 detail 拆出）
frontend/src/pages/ReadingStation.tsx                 # 新建主壳
frontend/src/components/reading/                      # 新建子目录
  ├── PdfViewer.tsx
  ├── SpeedCardPane.tsx
  ├── ClaimCard.tsx                                   # 三态（含引文展开）
  ├── EvidenceItem.tsx                                # 5 类 material 模板
  ├── NotesPane.tsx
  ├── CommentList.tsx
  ├── CommentForm.tsx
  ├── TagEditor.tsx
  ├── BacklinkEditor.tsx
  ├── ActionBar.tsx                                   # 状态机驱动
  └── StatusPill.tsx                                  # 顶部双入口
frontend/src/api/papers.ts                            # 新增 useHasPdf / list typeahead 等
frontend/src/api/claims.ts                            # 新建（仅留接口；ft-032 才有 patch）
frontend/src/types/paper.ts                           # 加 has_pdf / pdf_url / evidences / counter_signals
frontend/src/lib/statusMachine.ts                     # 新建：STATUS_TRANSITIONS 镜像 + canTransition()
frontend/package.json                                 # 加 react-pdf + react-resizable-panels
frontend/vite.config.ts                               # 如需 worker chunk 配置
```

## 文件黑名单（严禁触碰）

```
apps/                                                 # backend 不动
electron/                                             # Electron 不动
docs/                                                 # PM 文档不动
frontend/src/pages/PaperListPage.tsx                  # ft-028 ✅，本 ft 仅可读不改
frontend/src/components/VerdictActions.tsx            # 同上
frontend/src/components/StatusFilterBar.tsx           # 同上
pyproject.toml / uv.lock                              # 不动
```

## 依赖增加（仅这两个）

```jsonc
"react-pdf": "^9.x",            // 包含 pdfjs-dist
"react-resizable-panels": "^2.x"
```

## 实施阶段（建议顺序，允许部分完成）

### Phase 1：基础设施（必做，~1.5 天）

1. 安装两个新依赖（`npm install` 在沙箱可跑）
2. `types/paper.ts` 扩 has_pdf / pdf_url / Evidence / counter_signals
3. `lib/statusMachine.ts`：5 态 + 镜像后端 STATUS_TRANSITIONS
4. `PaperDetailPage.tsx` 改壳：`useHasPdf` (HEAD) + station/speed 分发
5. `SpeedReadView.tsx`：把当前 PaperDetailPage 速读卡内容拆出（**不重写**，只搬位置 + 纯函数化）
6. `ReadingStation.tsx` 三栏空壳（`react-resizable-panels`）+ 响应式（窄屏 < 1100px notes 折抽屉）
7. `PdfViewer.tsx` hello-world：能加载 `/api/papers/<id>/pdf/` 显示首页
   - **必须先验证 worker 配置可用**（react-pdf v9 + Vite 5 已知坑）
   - vite `?url` import worker bundle

**Phase 1 验收**：`npm run typecheck` 0 error；station 路由打开能看见三栏 + PDF 首页

### Phase 2：ClaimCard 三态 + 引文展开（必做，~1.5 天）

8. `SpeedCardPane.tsx` 用既有速读内容（窄列 + figure 缩略网格）
9. `ClaimCard.tsx`：collapsed / expanded with evidence 两态
10. `EvidenceItem.tsx`：5 类 material 模板（section / figure / table / equation / citation）
11. 编辑入口 `[✏]` placeholder（toast "需 ft-032"，disabled state）
12. `[⋯]` 菜单 placeholder（Hide claim 同样 toast）
13. `[→ jump in PDF]` 联动 PdfViewer ref（scrollToPage / highlightBox / searchText）

**Phase 2 验收**：claim `▸` 展开能看见 evidence 列表 + 5 类不同模板正确

### Phase 3：PdfViewer 联动核心（~1.5 天）

14. `scrollToPage(page)` —— 虚拟滚动 / Page 组件
15. `highlightBox(page, bbox)` —— absolute overlay div + 2s 渐隐 + 坐标转换（PDF points 左下 → canvas px 左上 + scale）
16. `searchText(query)` —— pdf.js findController
17. textLayer renderTextLayer + z-index 不挡 highlight overlay

**Phase 3 验收**：claim:fig → 跳页 + 框 2s；claim:sec → 文本搜索高亮

### Phase 4：NotesPane（~2 天）

18. `NotesPane.tsx` Tabs 壳（Comments / Tags / Backlinks）
19. `CommentList` + `CommentForm`（append-only，时间序，Cmd+Enter 提交，hidden 灰色折叠）
20. `TagEditor`（复用 ft-027 ChipInput）
21. `BacklinkEditor`（outgoing remove / incoming readonly / typeahead 搜索 add，调 `?q=` icontains）

**Phase 4 验收**：三 tab CRUD 都跑通；comment 时间序正确

### Phase 5：状态机 action bar + status pill（~0.5 天）

22. `ActionBar.tsx`：状态机驱动按钮组（reading 4 键 / read_*/archived 简化）
   - reading: `[Mark kept] [Mark dropped] [Snooze] [Archive]`
   - read_kept|dropped: `[Archive] [Reopen]`
   - archived: `[Reopen]`
   - **纯文字无图标**
23. `StatusPill.tsx`：顶部右上 `[reading ▾]` Dropdown，5 态全集，受 canTransition 约束 disable
24. 自动 status bump：ReadingStation 挂载时 new/queued → reading

**Phase 5 验收**：状态切换正确；冗余双入口都生效

### Phase 6：收尾（~0.5 天）

25. PDF 不可用时降级 2 栏（speed-card 60% + notes 40%）
26. resize 持久（react-resizable-panels `autoSaveId`）
27. code-split：`PaperDetailPage` 内 lazy import `ReadingStation`（vite 自动 split）
28. 顶部 mode toggle `[Speed] / [Station]` localStorage 持久
29. `npm run build` 验主 chunk 体积；如爆走 `manualChunks` 切

## 跨引契约（关键）

`SpeedCardPane` 暴露 `onCiteClick(materialId: string)` callback，`ReadingStation` 用 ref 转发到 `PdfViewer`：

```tsx
function handleCite(materialId: string) {
  const fig = detail.figures.find(f => f.material_id === materialId);
  if (fig) {
    pdfRef.current?.scrollToPage(fig.page);
    if (fig.bbox) pdfRef.current?.highlightBox(fig.page, fig.bbox);
    return;
  }
  const tbl = detail.tables.find(t => t.material_id === materialId);
  if (tbl) {
    pdfRef.current?.scrollToPage(tbl.page);
    return;
  }
  const sec = detail.sections.find(s => s.material_id === materialId);
  if (sec) {
    pdfRef.current?.searchText(sec.path.slice(0, 60));
    return;
  }
  // equation / citation 不强联动
}
```

## DTO 实测（取自 rpt-013）

```json
{
  "arxiv_id": "2401.10001",
  "paper_key": "ABCD2345",
  "title": "...",
  "status": "new",
  "tags": [],
  "n_comments": 0,
  "has_pdf": true,
  "pdf_url": "/api/papers/ABCD2345/pdf/",
  "sections": [...],
  "figures": [...],
  "tables": [...],
  "equations": [...],
  "claims": [
    {
      "claim_id": "2401.10001:claim:1",
      "text": "...",
      "claim_type": "finding",
      "source_section_path": "Methods / 3.2 Training",
      "confidence": 0.87,
      "evidences": [
        {"material_id": "2401.10001:sec:Methods/3.2", "relation": "supports"},
        {"material_id": "2401.10001:fig:4", "relation": "supports"}
      ],
      "counter_signals": []
    }
  ]
}
```

`HEAD /api/papers/<id>/pdf/` → 200/404；`GET` 同 endpoint 返 application/pdf 全量 body（无 Range，论文 < 10MB OK）。

## 验收（最终）

详见 `features/ft-029.md#验收`（13 项）。本派单 5 个 phase 都完成才算 fully done。

## 报告

完成后**新建** `docs/pm/communications/rpt-014.md`：

```yaml
---
pm_id: rpt-014
pm_type: report
workstream: frontend
feature: ft-029
dispatch_ref: dsp-014
status: completed   # 或 in-progress（部分完成）
created_at: 2026-04-29
---
```

报告内容：
- 完成的 phase（1/2/3/4/5/6）+ 每个 phase 的关键文件清单
- **未完成的 phase**（如有）+ 阻塞原因 + 建议后续会话怎么续
- `npm run typecheck` 结果（**必须 0 error**）
- `npm run build` 结果 + 主 chunk 体量
- 已知 visual / behavior gap（用户实测会发现的）
- 主会话 commit 草稿（一组 phases 一个 commit 也可）

**优先级原则**：如果 token 不够全做完，优先级 Phase 1+2+3 > Phase 4 > Phase 5+6。Phase 1+2 至少跑完，让用户能视觉 review ClaimCard 三态 + 引文展开（这是 4/28 PM review 的核心增强）。

完成后简短回我（< 200 字）：完成 phase / 体积 / 已知 gap / 是否需要派后续 dispatch。
