---
pm_id: rpt-003
pm_type: report
workstream: backend-extract
feature: ft-015
dispatch_ref: dsp-003
status: completed
created_at: 2026-04-26
---

# rpt-003：ft-015 Docling-based PDF 解析升级 完成

## 交付
- `apps/extract/extractors/docling_ext.py` — `DoclingExtractor` 实现 Protocol，模块级单例 + Lock + paper-level 缓存
- `apps/extract/tests_docling_ext.py` — 11 mock 用例覆盖 5 类映射 / 无图跳过 / API 兼容 / 缓存幂等
- `apps/extract/extractor.py` — `DefaultExtractor` 委托到 DoclingExtractor
- 删除 `equation_extractor.py` / `citation_extractor.py` 启发式 + 测试

## 验证
- `pytest apps/extract/`: 28 passed
- 全量回归: 146 passed
- ruff: 全过
- 单测全 mock，不真跑 docling
- **真实 PDF 验证**（LeWorldModel.pdf）：
  - sections=33 / figures=19（每条带 image_path，PNG 文件存在）/ tables=9 / equations=31（全 LaTeX）/ citations=54（含 bibkey+year）
  - 完美命中 probe 数据
- 合并 commit: pending（待主会话）

## 偏离 dispatch（已接受）
agent 选择**保留** `section_extractor.py` / `caption_extractor.py` / `figure_extractor.py` 三个旧 façade，不改写为 docling-backed。
理由：
- API 已是 façade 形态且被 `subscriptions/run_subscription.py` + `interpret/tests_pdf_chunker.py` 共同消费（不在 dsp-003 白名单）
- `chunk_pdf(arxiv_id, pdf_path, cache_dir=None)` 签名修改会破坏既有契约
- 两条路径职责正交：docling 产 5 类 material（v0.6/0.7 解读器消费），pymupdf 产 LLM 走料 markdown（既有 deep_interpret 链路）

后续若要迁移既有 LLM 走料路径到读 DB，另起 dispatch（涉及 run_subscription / interpret/tests_pdf_chunker 白名单扩容）。

## 后续
- iter-004 即将关闭（ft-019 ✅ + ft-022 ✅ + ft-015 ✅）
- iter-005 准备 ft-020 解读器 L1+L2（直接读 `extract_*` 表，材料底座质量已验证）
