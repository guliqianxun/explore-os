---
pm_id: dsp-006
pm_type: dispatch
target: backend-core
action: develop
feature: ft-022
priority: P0
status: pending
created_at: 2026-04-26
deadline: 2026-05-03
branch: feat/ft-022-electron-infra
---

# 任务: Electron 基础设施清理 + DRF API（ft-022）

## 背景

iter-006 ft-021 落地的图谱可视化实测可读性不及 HTML，二次决策图谱推迟到 v1.x。
本任务把现有 CLI 包装成 DRF REST API + 清理单机化耦合点（C1/C3/C5/C9），
为 ft-023 Electron shell + sidecar 铺路。

完整 spec 见 [features/ft-022.md](../features/ft-022.md)。

## 严格边界

**仅允许触碰**：
- 新建 `apps/core/**`、`apps/api/**`
- 修改 `config/settings.py`（DATA_DIR / MEDIA_ROOT / DATABASES default）
- 修改 `config/urls.py`（include api）
- 修改 `pyproject.toml` 添加 `apscheduler>=3.10`
- 替换 hardcoded `BASE_DIR / "media"` 的位置（最小 patch）：
  - `subscriptions/memory.py`
  - `sources/pdf_fetcher.py`
  - `apps/extract/extractors/docling_ext.py` 的 `_figures_dir`
  - `apps/render/graph.py` 的 `_equation_image_path`
  - `apps/render/management/commands/render_graph.py` 的 out_dir 计算
  - `delivery/adapters/email.py`（SMTP fallback）

**严禁触碰**：
- `apps/extract/base.py`、`apps/extract/models.py`、`apps/extract/extractors/docling_ext.py` **的 mapper / extract 逻辑**（只允许改路径函数）
- `apps/interpret/base.py`、`apps/interpret/models.py`、`apps/interpret/interpreter.py`、`apps/interpret/persist.py` 业务逻辑
- `apps/render/base.py`、`apps/render/graph.py` 的 `build_graph()` 业务逻辑、`apps/render/excalidraw_renderer.py`、`apps/render/svg_renderer.py`、`apps/render/equation_render.py`
- `interpret/**` legacy
- `frontend/**`（不存在，不要创建）
- `electron/**`（不存在，不要创建，留 ft-023）
- `docs/**`

## 文件白名单

**新增**：
- `apps/core/__init__.py` / `apps.py` (label="core")
- `apps/core/paths.py` — 集中路径函数：`data_dir() / media_dir() / papers_dir() / figures_dir(arxiv_id) / equations_dir(arxiv_id) / render_dir(arxiv_id) / cache_dir() / outbox_dir()`
- `apps/core/scheduler.py` — `get_scheduler()` 单例 BackgroundScheduler + `register_default_jobs()`
- `apps/core/tests_paths.py`
- `apps/core/tests_scheduler.py`
- `apps/api/__init__.py` / `apps.py` (label="api")
- `apps/api/views.py` / `serializers.py` / `urls.py`
- `apps/api/jobs.py` — 简单的 job queue 抽象（基于 APScheduler one-shot job）
- `apps/api/tests_views.py`
- `apps/api/tests_jobs.py`

**修改（最小 patch）**：
- `config/settings.py`：
  - 新增 `DATA_DIR = _resolve_data_dir()`（环境变量 `EXPLORE_OS_DATA_DIR` 优先；否则跨平台默认 `%APPDATA%/explore-os` / macOS Application Support / `~/.config/explore-os`）
  - `MEDIA_ROOT = DATA_DIR / "media"`
  - `DATABASES["default"] = env.db("DATABASE_URL", default=f"sqlite:///{DATA_DIR / 'explore_os.sqlite3'}")`
  - `INSTALLED_APPS` 追加 `"apps.core"` + `"apps.api"`
  - 顶部导入 `apps.core.paths` 在 docling 任何 import 之前**（HF_HOME 重定向时机要求）**
- `config/urls.py`：`include("apps.api.urls")` 到 `path("api/")`
- `pyproject.toml`：`apscheduler>=3.10` 加到 dependencies
- 6 个旧位置的路径调用（见 spec 「替换 hardcoded」清单）改用 `apps.core.paths` 函数
- `delivery/adapters/email.py`：`EMAIL_HOST=""` 时写 `.eml` 到 `outbox_dir()`

## 接口契约

```python
# apps/core/paths.py
from pathlib import Path
from django.conf import settings

def data_dir() -> Path: ...
def media_dir() -> Path: ...
def papers_dir() -> Path: ...
def figures_dir(arxiv_id: str) -> Path: ...
def equations_dir(arxiv_id: str) -> Path: ...
def render_dir(arxiv_id: str) -> Path: ...
def cache_dir() -> Path: ...
def outbox_dir() -> Path: ...

# 顶部还有路径副作用：设置 HF_HOME / TRANSFORMERS_CACHE 环境变量
# 必须在 import docling 之前生效
import os
_CACHE = data_dir() / "cache" / "huggingface"
os.environ.setdefault("HF_HOME", str(_CACHE))
os.environ.setdefault("TRANSFORMERS_CACHE", str(_CACHE / "transformers"))
```

```python
# apps/core/scheduler.py
from apscheduler.schedulers.background import BackgroundScheduler

_SCHEDULER: BackgroundScheduler | None = None

def get_scheduler() -> BackgroundScheduler:
    """返回 in-process 单例 scheduler。"""

def start_scheduler(): ...   # 启 sidecar 时调
def shutdown_scheduler(): ...  # 关 sidecar 时调
```

```python
# apps/api/urls.py 路由
urlpatterns = [
    path("health/", HealthView.as_view()),
    path("papers/", PaperListView.as_view()),
    path("papers/<str:arxiv_id>/", PaperDetailView.as_view()),
    path("papers/<str:arxiv_id>/markdown/", PaperMarkdownView.as_view()),
    path("papers/<str:arxiv_id>/figure/<int:seq>.png", FigureView.as_view()),
    path("papers/<str:arxiv_id>/claims/", ClaimsView.as_view()),
    path("papers/<str:arxiv_id>/extract/", ExtractTriggerView.as_view()),
    path("papers/<str:arxiv_id>/interpret/", InterpretTriggerView.as_view()),
    path("papers/<str:arxiv_id>/render/", RenderTriggerView.as_view()),
    path("jobs/<str:job_id>/", JobStatusView.as_view()),
]
```

## 测试要求

1. **paths**：用 `monkeypatch.setenv("EXPLORE_OS_DATA_DIR", str(tmp_path))` 测所有路径函数指向 tmp_path 子目录
2. **scheduler**：测 singleton + start/shutdown 不抛
3. **API views**：用 `rest_framework.test.APIClient` 测 8 endpoint 的 happy path（mock 业务调用）
4. **paths 重定向不破业务**：在 tmp DATA_DIR 下跑 `extract_paper` mock 走通（mock _convert）
5. **既有测试不退化**：全量回归至少 214

## 验收

1. `EXPLORE_OS_DATA_DIR=/tmp/x uv run python manage.py migrate` 创建 `/tmp/x/explore_os.sqlite3`
2. `EMAIL_HOST="" uv run python manage.py run_subscription ...` 写 `/tmp/x/outbox/*.eml`
3. `uv run python -c "from apps.core.scheduler import get_scheduler; s=get_scheduler(); print(s.running)"` 不抛
4. `curl http://127.0.0.1:8000/api/health/` 返回 200（用 dev server 实测）
5. 全量 pytest 绿（≥ 214 + 新测试）
6. ruff 通过
7. **既有 CLI 全不破**：`extract_paper` / `interpret_paper` / `render_graph` / `run_subscription` 全部能跑

## 工作流程

1. 读 `/CLAUDE.md`（特别是「长期形态约束」「桌面端栈」）
2. 读 `docs/pm/communications/dsp-006.md`（本任务）
3. 读 `docs/pm/features/ft-022.md`（feature spec）
4. 读 `docs/pm/iterations/iter-007.md`（sprint 上下文）
5. 读现有路径硬编码位置 + 现有 CLI command 文件（理解要包装的业务）
6. 写 `apps/core/paths.py` + 替换硬编码（先做这条，验证既有 CLI 仍跑）
7. 写 `apps/core/scheduler.py`
8. 写 `apps/api/views.py` 等 8 endpoint
9. 写 SMTP fallback
10. 测试 + ruff + 全量回归
11. **不 commit**（沙箱限制）

## 不要做

- 不要碰 `apps/{extract,interpret,render}/**` 的业务模型 / mapper
- 不要新建 frontend / electron 目录
- 不要改 docs/
- 不要安装 frontend 依赖
- 不要 git commit

## 参考

- Feature spec: [features/ft-022.md](../features/ft-022.md)
- Iteration: [iterations/iter-007.md](../iterations/iter-007.md)
- 项目约束: `/CLAUDE.md`
