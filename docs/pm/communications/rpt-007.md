---
pm_id: rpt-007
pm_type: report
workstream: frontend-shell
feature: ft-023
dispatch_ref: dsp-007
status: completed
created_at: 2026-04-26
---

# rpt-007：ft-023 Electron shell + Python sidecar 完成

## 交付
- `sidecar_entry.py`：Waitress 跑 Django，`--port 0` OS 分配，stdout 打印 listening banner
- `electron/` 整目录：main / sidecar / preload / port / types TS 文件 + placeholder.html + electron-builder.yml + icons 占位
- `build/sidecar-cuda.spec` + `build/hooks/hook-torch.py`：PyInstaller 配置
- pyproject.toml 加 pyinstaller + waitress dev-dep
- .gitignore 排除 electron/dist / node_modules / build/dist

## 验证（沙箱内）
- `uv sync` 成功（新增 6 包）
- `import sidecar_entry` OK
- waitress 实测：banner `listening on http://127.0.0.1:63919` + `/api/health/` 200 + DATA_DIR 透传
- 全量 pytest 241 passed（零回归）

## 偏离 / 修正
- **waitress 3.0.2 API 修正**：dispatch 伪码 `server.sockets[0].getsockname()[1]` 不存在；改用 `server.socket` 兜底 + MultiSocketServer `effective_listen` 兜底

## 沙箱未验证（用户本机）
1. `cd electron && npm install`
2. dev：`npm run dev:electron` 启动 Electron 窗口 + 占位页 + sidecar
3. build：`npm run build:all` 产 CUDA NSIS 安装包（预期 1-1.5GB）
4. macOS DMG / Linux AppImage 跨平台

## 后续
- iter-008 关闭
- iter-009 准备 ft-024（前端 MVP 4 页：Vite+React+TS+Tailwind+shadcn/ui+KaTeX）
- 用户本机首次跑通 Electron shell 后再开 ft-024
