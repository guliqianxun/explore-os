---
pm_id: rpt-005
pm_type: report
workstream: backend-render
feature: ft-021
dispatch_ref: dsp-005
status: completed
created_at: 2026-04-26
---

# rpt-005：ft-021 图谱抽象层 + Excalidraw renderer 完成

## 交付
- `apps/render/` Django app（label="render"，db_table="render_artifacts"）
- 抽象层：`PaperGraphModel` + `GraphRenderer` Protocol + `build_graph()`
- ExcalidrawRenderer + SvgRenderer + 共用 `layout.py` 三行分层
- CLI: `render_graph <arxiv_id> [--format excalidraw|svg]`
- 22 测试用例

## 验证
- `pytest apps/render/`: 22 passed
- 全量回归: 206 passed (184 → 206)
- ruff 通过
- **真实实战两篇全跑通**：
  - LeWorldModel: 19 nodes / 16 edges / 631KB / 7 image base64
  - ConvNeXt V2:  17 nodes / 12 edges / 968KB / 5 image base64
  - SVG fallback: 同 leworldmodel 结构，节点 marker arrow 边色正确

## 设计亮点
- counter_signal 简化为 contradicts edge attached to claim（不作为独立节点），图谱聚焦
- node_id 简写（claim:1）↔ extract material_id（<arxiv_id>:figure:3）双向映射，跨 app 边界清晰
- 边色固定：supports 蓝 / illustrates 绿 / contradicts 红 dashed / cites 灰
- 同库前缀 render_*，全 JSONField，SQLite 友好
- Excalidraw 标准 schema：rectangle + image + text containerId 绑定 + arrow startBinding/endBinding

## 偏离 / 注意
- 无功能性偏离
- worktree stale 基线问题：agent 沙箱给的 worktree 没有 ft-019/020 baseline，agent 用绝对路径回主 checkout 落地（CLAUDE.md #1 现场重现）。主会话直接在主仓 commit，无副作用

## 后续
- iter-006 关闭
- iter-007 准备 ft-022（Electron 基础设施清理 + DRF API）—— v0.9 桌面化第一步
- 三段中台已完整闭环（抽取 ✅ + 解读 ✅ + 渲染 ✅）
