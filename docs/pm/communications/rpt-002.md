---
pm_id: rpt-002
pm_type: report
workstream: research
feature: ft-022
dispatch_ref: dsp-002
status: completed
created_at: 2026-04-25
---

# rpt-002：ft-022 单机 app packaging 调研 完成

## 交付
- `docs/architecture/packaging.md`（354 行，7 章 + 推荐路径 + 参考链接）
- merge commit `dec8d7c`

## 主结论
**Tauri 2.x + PyInstaller sidecar(Django) + SQLite + APScheduler in-process**。
Electron 兜底，切换成本 1–2 人日（前提：前端 day 1 用 `bridge.ts` 抽象 API）。

## 关键发现
1. **ORM 层 0 行 patch 即可切 SQLite**：grep `ArrayField`/`tsvector`/`RunSQL`/`raw_sql` 全 0；models.py 当前全空（数据持久化在 `media/**`）
2. **唯一硬阻塞**：`config/settings.py:60-65` 默认连 PG，切到 `appdirs.user_data_dir / explore_os.sqlite3`
3. **`BASE_DIR / "media"` 路径假设**是 frozen exe 最大坑（多处），需统一 `EXPLORE_OS_DATA_DIR`
4. **SMTP 必填假设**违反单机预期（`delivery/adapters/email.py:29`、`config/settings.py:87`），需 fallback 写 `.eml` 到下载目录
5. **包体预估**：100–180MB（Tauri）/ 180–270MB（Electron）；最大头 pymupdf/pymupdf4llm
6. **Tauri 失败决策点**已成 9 条可量化判定

## 后续 ft 建议
新建 ft-023 packaging 实施，分两步走：
- **Step 1**：单机化基础清理（C1/C3/C4/C5/C9 必修）+ 切 SQLite + APScheduler；不碰前端、不 PyInstaller；跑通 pytest
- **Step 2**：Tauri 壳 + 前端最小骨架（强制 `bridge.ts`）+ PyInstaller spec + CI 三平台 build

v1.0 启动时再开 ft-023。
