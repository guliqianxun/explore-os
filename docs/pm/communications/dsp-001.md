---
pm_id: dsp-001
pm_type: dispatch
target: backend-extract
action: develop
feature: ft-019
priority: P0
status: pending
created_at: 2026-04-25
deadline: 2026-05-09
branch: feat/ft-019-extract-layer
worktree: .claude/worktrees/ft-019
---

# 任务: 抽取器素材索引层（ft-019）

## 背景

把项目从「论文日报 pipeline」升级为「论文理解中台」的第一段。
当前 PDF/caption/figure 解析逻辑散落在 `interpret/` 下，需重构为独立 `apps/extract/` Django app，
五类 material（section / figure / table / equation / citation）落库为可寻址一等公民，
为后续 ft-020（解读器）/ ft-021（渲染器）提供素材底座。

完整 spec 见 [features/ft-019.md](../features/ft-019.md)。

## 严格边界

**抽取器只产确定性 material，不做语义。**
- ❌ 不要做 claim 抽取
- ❌ 不要做 figure 分类（architecture/result）
- ❌ 不要做 figure_picker 选图
- ❌ 不要触碰 `interpret/deep_interpret.py` / `narrative.py` / `tldr.py`
- ❌ 不要触碰 `delivery/` 任何文件
- ❌ 不要触碰 `sources/` 任何文件（PDF 拉取归 sources，仍由 sources 负责）

## 文件白名单（仅允许新增/修改）

**新增**：
- `apps/extract/__init__.py`
- `apps/extract/apps.py`
- `apps/extract/models.py`（5 个 model：Section/Figure/Table/Equation/Citation）
- `apps/extract/migrations/0001_initial.py`
- `apps/extract/base.py`（Extractor Protocol + dataclass 接口）
- `apps/extract/extractor.py`（统一调度入口）
- `apps/extract/section_extractor.py`（搬自 `interpret/pdf_chunker.py`）
- `apps/extract/caption_extractor.py`（搬自 `interpret/caption_extractor.py`）
- `apps/extract/figure_extractor.py`（搬自 `interpret/figure_extractor.py`）
- `apps/extract/equation_extractor.py`（新写：从 PDF 抽 display equation，最简版用 pymupdf 检测）
- `apps/extract/citation_extractor.py`（新写：从 references 段落抽 bibkey + 标题 + 年份）
- `apps/extract/management/__init__.py`
- `apps/extract/management/commands/__init__.py`
- `apps/extract/management/commands/extract_paper.py`
- `apps/extract/tests_extractor.py`（端到端）
- `apps/extract/tests_section_extractor.py`
- `apps/extract/tests_caption_extractor.py`
- `apps/extract/tests_figure_extractor.py`
- `apps/extract/tests_equation_extractor.py`
- `apps/extract/tests_citation_extractor.py`

**修改**（最小化）：
- `config/settings.py`：`INSTALLED_APPS` 加 `apps.extract`
- `interpret/__init__.py`：从此处移除被搬走模块的 import（如有）
- 旧 `interpret/pdf_chunker.py` / `caption_extractor.py` / `figure_extractor.py`：留 thin re-export wrapper（一行 `from apps.extract.xxx import *`）保证既有 import 不破，本 sprint 完成后可在 ft-020 中清理
- 旧 `interpret/tests_pdf_chunker.py` / `tests_caption_extractor.py`：保留并改 import 指向新位置

**严禁触碰**：
- `delivery/**`、`sources/**`、`subscriptions/**`
- `interpret/deep_interpret.py`、`interpret/narrative.py`、`interpret/tldr.py`、`interpret/figure_picker.py`、`interpret/figure_classifier.py`、`interpret/ranker.py`、`interpret/rewriter.py`、`interpret/embedding.py`、`interpret/interpretation.py`
- `docs/pm/**`（PM 文档由主会话维护）

## 关键约束

1. **同库不同前缀**：所有表 `db_table` 显式写 `extract_sections` / `extract_figures` / `extract_tables` / `extract_equations` / `extract_citations`
2. **避免 PG-only 字段**：用 `JSONField` 抽象 jsonb；禁用 `ArrayField`、PG `tsvector`；migrations 不写 raw SQL；写 `update_or_create` 而不是 `bulk_insert ON CONFLICT`
3. **material_id 规则**：`<paper_arxiv_id>:<type>:<seq>`，例 `2401.12345:figure:3`。seq 按 paper 内顺序稳定编号
4. **幂等**：同一 paper 重复 extract，material_id 不变，DB 走 `update_or_create`
5. **Citation MVP**：仅存 bibkey + 标题 + 年份；arXiv id / DOI 反解延后

## 接口契约（必须严格按这个写）

```python
# apps/extract/base.py
from typing import Protocol
from dataclasses import dataclass, field
from pathlib import Path

@dataclass
class BaseMaterial:
    material_id: str
    paper_arxiv_id: str
    type: str       # section | figure | table | equation | citation
    raw_payload: dict = field(default_factory=dict)

@dataclass
class SectionMaterial(BaseMaterial):
    path: str = ""        # "3.2 Method"
    level: int = 0
    char_offset_start: int = 0
    char_offset_end: int = 0
    raw_text: str = ""

@dataclass
class FigureMaterial(BaseMaterial):
    fig_label: str = ""
    page: int = 0
    bbox: list[float] | None = None
    caption: str = ""
    image_path: str = ""

@dataclass
class TableMaterial(BaseMaterial):
    tbl_label: str = ""
    page: int = 0
    bbox: list[float] | None = None
    caption: str = ""
    raw_text: str = ""

@dataclass
class EquationMaterial(BaseMaterial):
    eq_label: str | None = None
    page: int = 0
    bbox: list[float] | None = None
    latex_or_text: str = ""
    inline_or_display: str = "display"

@dataclass
class CitationMaterial(BaseMaterial):
    bibkey: str = ""
    raw_text: str = ""
    title: str = ""
    year: int | None = None

@dataclass
class ExtractResult:
    sections: list[SectionMaterial]
    figures: list[FigureMaterial]
    tables: list[TableMaterial]
    equations: list[EquationMaterial]
    citations: list[CitationMaterial]

class Extractor(Protocol):
    def extract(self, paper_pdf_path: Path, paper_arxiv_id: str) -> ExtractResult: ...
```

## 验收标准

1. `uv run python manage.py migrate` 通过，新增 5 张 `extract_*` 表
2. `uv run python manage.py extract_paper 2401.12345`（用 ft-013/014 已有缓存 PDF 测试）写入五类 material
3. 重复 extract 同一篇幂等：DB 行数不变，material_id 不变
4. 既有 `run_subscription` 仍能产出邮件（不要因为搬迁破坏 deep_interpret 链路）
5. 所有新测试 + 既有测试全绿：`uv run pytest`
6. ruff 通过：`uv run ruff check apps/extract/`

## 工作流程

1. 读 `docs/pm/features/ft-019.md` 完整 spec
2. 读 CLAUDE.md「长期形态约束」与「并行开发：worktree + subagent 协作规范」
3. 读现有 `interpret/pdf_chunker.py` / `caption_extractor.py` / `figure_extractor.py` 理解搬迁内容
4. 先建 `apps/extract/` 骨架 + base.py 接口 + models.py + migrations
5. 搬迁三个模块（保持原逻辑，仅改 import 与归属）
6. 新写 equation_extractor / citation_extractor（最简实现，不追求高召回）
7. 写测试 + CLI
8. 跑 `uv run pytest` + `uv run ruff check`
9. **Subagent 沙箱不能 git commit，由主会话代为 commit**——任务结束时把改动清单 + 关键文件路径列出来即可

## 不要做

- 不要重新设计接口（必须严格按上面给的 dataclass）
- 不要顺手清理 `interpret/` 下其他文件
- 不要改 ROADMAP / index.json / 任何 docs/pm/**
- 不要写 README 或单独的设计文档（spec 已在 ft-019.md）
- 不要 git commit（沙箱权限，主会话代办）

## 参考

- Feature spec: [features/ft-019.md](../features/ft-019.md)
- Iteration: [iterations/iter-004.md](../iterations/iter-004.md)
- 项目约束: `/CLAUDE.md`
