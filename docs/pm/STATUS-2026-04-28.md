---
pm_id: status-2026-04-28
pm_type: snapshot
generated_at: 2026-04-28
---

# explore-os Status Snapshot · 2026-04-28（开发会话收敛）

## 一句话现状

**ft-028 全套落地（Paper-centric schema + user_* 4 表 + 11 DRF endpoints + Inbox verdict UI），dev 端到端 verdict API smoke 通；同日砍 PostgreSQL 转 SQLite 单进；速读卡片 pivot + Electron 标题栏 + figure 归属 fix 三个 ft-026 follow-up 合 commit；ft-029 Reading Station spec 立项待派发。**

## 今日 commit 链（8 个 commit / +2380 -700 lines）

```
6b232a7  chore         砍 PG（psycopg + docker-compose + 文档同步）
25bfd08  docs(pm)      ft-028 收尾（rpt-011/012 关 dsp）+ ft-029 立项
21d95a1  feat(backend) Paper-centric schema + user_* + 11 DRF endpoints
d09e6b9  feat(frontend) Inbox verdict UI（VerdictActions+StatusFilterBar+PaperList）
0b9355b  docs(pm)      ft-028 立项 + iter-012 + dsp-011/012 双 agent 派发
b8842a7  feat(frontend) PaperDetail → 速读卡片 pivot（产品决策级）
c73a71d  feat(electron) frameless 标题栏 + sidecar 优先 venv + NO_PROXY 兜底
daaf73f  fix(extract)  docling _map_sections 累积 raw_text + 相似度 path 纳入
```

## 重大决策 lock（4/28）

| 决策 | 含义 |
|---|---|
| **A 进 B 出 product shape** | brief 入口（既有）+ 论文沉淀层（status/comment/tag/双链）|
| **Paper-centric schema** | `[A-Z2-9]{8}` Zotero 风格 stable_key + title + arxiv_id/doi 元数据列；5 类 material + 解释层 FK 全切 Paper.id |
| **5 态状态机** | `new → queued → reading → read_kept / read_dropped → archived`（snoozed 并入 queued）|
| **comment append-only** | 仅 PATCH `hidden`，不允许编辑/删除 |
| **backlink 先 paper 颗粒度** | claim 级延后；先验证 paper 级是否有用 |
| **内嵌 pdf.js"对照型"viewer** | 不做标注；标注层全部去 user_comment / user_tag / user_backlink，挂 claim/section/figure |
| **Zotero/readest 是逃生口** | 一键导出 .bib + pdf；不重造 Zotero 的 archival/citation 库 |
| **砍 Postgres**（4/28 二次确认）| 桌面 app 单进 SQLite；psycopg / docker-compose / .env DATABASE_URL 全删 |
| **PaperDetail 形态级 pivot** | markdown 重渲全文 → 速读卡片：title + abstract + ClaimCard 排序 + figure 画廊；删 4 组件 |

## 里程碑进度

| Milestone | 内容 | 状态 |
|---|---|---|
| v0.1–v0.5 (M1–M5) | MVP / rerank / PDF+图 / caption+bbox / DeliveryAdapter | ✅ done |
| v0.6 (M5) | 抽取层 5 类 material + Docling | ✅ done |
| v0.7 (M6) | 解读器 L1+L2 | ✅ done |
| v0.8 (M7) | 图谱 + Excalidraw（freeze）| ✅ done frozen |
| v0.9 (M8) | Electron 基础设施 + DRF API + EXPLORE_OS_DATA_DIR | ✅ done |
| v1.0 (M9) | Electron shell + sidecar + PyInstaller | ✅ done |
| v1.1 (M10) | 前端 MVP 4 页 → Editorial 重设计 → Subscription/Ingest | 🔄 ft-024 ✅ / ft-026 ✅ / ft-027 待最终实测 |
| **v1.2 (M11)** | **用户层（Paper-centric + Inbox verdict + Reading Station + Library）** | 🔄 **ft-028 ✅** / ft-029 spec ✅ / ft-030 / ft-031 planned |
| v1.3 (M12) | 自动更新 + CUDA/CPU 双轨打包（ft-025 顺延）| 📋 planned |

## Features 状态

**Done (25)**：ft-001 ~ ft-014（v0.1–v0.4）/ ft-015 / ft-016 / ft-019 / ft-020 / ft-021 / ft-022 / ft-023 / ft-024 / ft-026 / **ft-028（today）**

**In progress (1)**：
- **ft-027** Subscription 表单化 + Ingest（代码 ✅，用户最终 ingest 链路实测待确认）

**Planned (4)**：
- **ft-029** Reading Station：3 栏 + pdf.js 内嵌 + notes pane（spec ✅，待派发 dsp-013/014）
- **ft-030** Library + FTS5 + Zotero export（v1.2）
- **ft-031** 桌面通知 + brief 未决分组（v1.2 polish）
- **ft-025** 自动更新 + CUDA/CPU 双轨（v1.3）

**Deferred (3)**：ft-012（superseded）/ ft-017 飞书 / ft-018 微信

## Sprint / Iteration

| Iter | 范围 | 状态 |
|---|---|---|
| iter-001 ~ iter-010 | MVP → ft-026 编辑设计 | ✅ done |
| iter-011 | ft-027 subscription 表单化 + Ingest | 🔄 待用户最终实测 |
| **iter-012** | **ft-028 Paper-centric schema + Inbox verdict** | ✅ **today done** |
| iter-013 | ft-029 Reading Station + pdf.js | 📋 spec 写好待启动 |
| iter-014 | ft-030 Library + Zotero export | 📋 planned |
| iter-015 | ft-031 通知 polish | 📋 planned |
| iter-016 | ft-025 自动更新 + CUDA/CPU 双轨 | 📋 planned |

## 测试 / 构建基线

| 项 | 现状 |
|---|---|
| pytest（默认 SQLite）| **306 passed / 0 fail / 0 error** in 18s（基线 262 + 44 新）|
| frontend tsc strict | 0 error |
| frontend bundle | 634KB / gzip 199KB（< ft-026 800KB 基线）|
| Electron tsc | 0 error |
| docker compose | 已删，不再依赖外部 DB |
| dev 端到端 smoke | API verdict POST/GET 持久化 ✅；浏览器 UI 用户实测中 |

## ft-028 端到端实测进展

✅ **通过**：
- DATA_DIR `%APPDATA%/Roaming/explore-os` 复用既有 SQLite，零数据迁移损失
- 既有 paper `fa28a00bdbc067db` 平稳迁成 `Paper(key=HSBFAON7, status=new)`
- 5 类 material + interpret 三表 paper FK 全部就位（`signal pre_save` 自动 wire）
- 11 endpoint API smoke 测通：`POST /api/papers/HSBFAON7/status/` 持久化 + `?status=` filter 工作
- frontend `[Skip][Queue][Read now]` 按钮联动后端

⚠️ **发现的 UX gap**（用户指出）：
- VerdictActions 三按钮覆盖 `read_dropped/queued/reading`，**没出口到 read_kept/archived**
- 详情页（速读卡）顶栏没有"读完了"按钮
- → **留 ft-029 Reading Station 设计**（spec 已规划底部 action bar `[Mark kept ✓][Mark dropped ✗][Archive]`，详情页结构化重做时一并落地）

## 战略锁定（CLAUDE.md 长期形态约束的延伸）

- **桌面端**：Electron unsigned 自用 + CUDA bundle 自用 + CPU 留分发接口
- **数据层**：**SQLite-only**（4/28 砍 PG）；同库前缀 `extract_*` / `interpret_*` / `render_*` / `papers_*` / `papers_user_*`
- **Paper 实体**：8-char Zotero 风格 stable_key + arxiv_id/doi 降为可选元数据
- **前端栈**：Vite + React 18 + TS strict + Tailwind 3 + shadcn/ui + KaTeX + Zustand + TanStack Query + (新增 ft-029) **pdf.js 内嵌 viewer**
- **设计调性**：Editorial / Magazine（Distill × Stratechery）
- **逃生口**：导出 .bib + pdf 到 Zotero；URL handler 在 readest 打开

## 已知 follow-ups（不阻塞）

1. **[Mark kept]/[Archive] UX gap** → ft-029 Reading Station 落地
2. **CLI 默认 PDF 路径** 仍读 BASE_DIR/media/pdf（paths.pdf_legacy_dir 现成，一行 patch 后续 ft）
3. **bundle code-splitting** → ft-029 接 pdf.js 时一并做（lazy import detail 页路由）
4. **`apps/render/*` 仍用 `paper_arxiv_id` 字符串关联** → 不影响功能，后续 ft 收尾
5. **legacy `interpret/*` 模块**（项目根的旧目录，非 `apps/interpret/`）未切 paper FK
6. **app.setName('explore-os')** 实际未生效但 DATA_DIR 仍是 `%APPDATA%/Roaming/explore-os`，原因待查（不阻塞）
7. **markdown 截断阈值 30K → 60-80K**（DashScope deepseek-v4-flash 上下文允许）
8. **subscription 切 DB**（v1.x 可选，目前 ruamel.yaml 保注释 OK）

## 下一次开发的起手式

1. 派 dsp-013（backend：Paper.pdf_path 字段 + ingest 自动填充 + `GET /api/papers/<id>/pdf/` FileResponse + tests）
2. 派 dsp-014（frontend：ReadingStation 全套 — 3 栏 + react-pdf + NotesPane + 跨引联动 + bottom action bar；spec 已锁，单 agent 不可拆）
3. 完成后 main session 集成 + 用户实测 + 若 OK 则关 iter-013 → 启动 iter-014 ft-030

参见 `features/ft-029.md`（spec）/ `iterations/iter-013.md`（待写，复刻 iter-012 模板即可）。
