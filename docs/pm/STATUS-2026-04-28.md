---
pm_id: status-2026-04-28
pm_type: snapshot
generated_at: 2026-04-28
---

# explore-os Status Snapshot · 2026-04-28

## 一句话现状

**ft-027 代码层完成（用户实测中）；ft-026 实测 follow-up 修了 figure 排版 + Electron 标题栏 + PaperDetail 形态级 pivot 到速读卡片；地基级 ft-028（Paper-centric schema + user_* 层 + Inbox verdict）立项中。**

## 重大产品决策（4/28 锁定）

### A 进 B 出 — 产品定位扩展

CLAUDE.md 原定位"订阅驱动的信息检索推送助手"扩展为：

- **A（入口）**：Subscription brief + Ingest（PDF/arxiv/URL）
- **B（沉淀）**：每篇论文挂 status / comment(history) / tag / 双链
- **能力切片**：内嵌 pdf.js 做"对照型"PDF 浏览（不做标注）；导出到 Zotero / 在 readest 打开是逃生口（不重造轮子）

### Paper-centric schema 重构（ft-028）

引入 `Paper` 顶层实体（Zotero 风格 8-char stable_key + 显示用 title +
arxiv_id/doi 降为可选元数据），既有 5 类 material + 解释层全部 FK 改指
`Paper.id`。一次性数据迁移 + API path 仍对外暴露 arxiv_id（不破现有
frontend）。

### 5 态状态机 + comment 历史

`new → queued → reading → read_kept / read_dropped → archived`
（snoozed 并入 queued）；comment 是 append-only 时间序列。

### 双链先 paper 颗粒度

backlink 只 `paper_key ↔ paper_key`（claim 颗粒延后，先验证 paper 级是否
有用）。

### PaperDetail 形态级 pivot（已落地，b8842a7）

markdown 重渲全文 → 速读卡片：title block + abstract + ClaimCard 列表
（claim_type 排序）+ figure 画廊（refed first / orphan after）。删 4 个
组件，不留废弃壳。

## 里程碑进度

| Milestone | 内容 | 状态 |
|---|---|---|
| v0.1–v0.5 (M1–M5) | MVP / rerank / PDF / caption+bbox / DeliveryAdapter | ✅ done |
| v0.6 (M5) | 抽取层 5 类 material + Docling | ✅ done |
| v0.7 (M6) | 解读器 L1+L2 | ✅ done |
| v0.8 (M7) | 图谱 + Excalidraw | ✅ done frozen |
| v0.9 (M8) | Electron 基础设施 + DRF API + EXPLORE_OS_DATA_DIR | ✅ done |
| v1.0 (M9) | Electron shell + sidecar + PyInstaller | ✅ done |
| v1.1 (M10) | 前端 MVP 4 页 → 编辑/杂志重设计 → subscription/ingest | 🔄 ft-024 ✅ / ft-026 ✅ / ft-027 in_progress |
| **v1.2 (M11)** | **用户层（Paper-centric schema + Inbox verdict + Reading Station + Library）** | 📋 ft-028 → ft-031 计划中 |
| v1.3 (M12) | 自动更新 + CUDA/CPU 双轨打包（ft-025 顺延） | 📋 planned |

## Features 状态

**Done (24)**：ft-001 ~ ft-014（v0.1–v0.4）/ ft-015 / ft-016 / ft-019 / ft-020 / ft-021 / ft-022 / ft-023 / ft-024 / ft-026

**In progress (1)**：
- **ft-027** Subscription 表单化 + Ingest（代码层 ✅，用户实测中）

**Planned (5)**：
- **ft-028** Paper-centric schema + user_* 层 + Inbox verdict（v1.2 地基）
- **ft-029** Reading Station：3 栏 + pdf.js 内嵌 + notes/tag/backlink（v1.2）
- **ft-030** Library：filter + FTS5 全文搜索 + Zotero export（v1.2）
- **ft-031** 桌面通知 + brief 未决分组（v1.2 polish）
- **ft-025** 自动更新 + CUDA/CPU 双轨（v1.3 顺延）

**Deferred (3)**：ft-012（superseded）/ ft-017 飞书 / ft-018 微信

## Sprint / Iteration

| Iter | 范围 | 状态 |
|---|---|---|
| iter-001 ~ iter-010 | MVP → ft-026 编辑设计 | ✅ done |
| iter-011 | ft-027 subscription 表单化 + Ingest | 🔄 待用户实测验收 |
| **iter-012** | **ft-028 Paper-centric schema + Inbox verdict** | 📋 即开 |
| iter-013 | ft-029 Reading Station + pdf.js | 📋 planned |
| iter-014 | ft-030 Library + Zotero export | 📋 planned |
| iter-015 | ft-031 通知 polish | 📋 planned |
| iter-016 | ft-025 自动更新 + CUDA/CPU 双轨 | 📋 planned |

## 测试 / 构建基线

- **Python pytest**: 262 passed
- **frontend npm run build**: 0 TS error
- **electron tsc**: 0 error
- **本机实测**：Vite + Electron + sidecar 三进程握手通过；速读卡片 SPEED
  READ + 标题 + abstract 渲染正常；figures 现已能按章节归属（fa28a00…：
  6/6 figures 全部命中，0 孤儿）

## 战略锁定

- **桌面端**：Electron unsigned 自用 + CUDA bundle 自用 + CPU 留分发接口
- **前端栈**：Vite + React 18 + TS strict + Tailwind 3 + shadcn/ui + KaTeX
  + Zustand + TanStack Query + (新增) **pdf.js 内嵌 viewer**
- **设计调性**：Editorial / Magazine（Distill × Stratechery）
- **数据层**：避免 PG-only，全 SQLite 友好；同库前缀 `extract_*` /
  `interpret_*` / `render_*` / **新增 `user_*`**
- **Paper 实体**：Zotero 风格 8-char stable_key + arxiv_id/doi 降为可选元数据
- **逃生口**：导出 .bib + pdf 到 Zotero；URL handler 在 readest 打开

## 已知 follow-ups（不阻塞）

1. CLI 默认 PDF 路径仍读 BASE_DIR/media/pdf（paths.pdf_legacy_dir 现成，一行 patch 后续 ft）
2. bundle 800KB（vite 警告 > 500KB，留 v1.x code-splitting）
3. app.setName('explore-os') 未生效（DATA_DIR 仍是 %APPDATA%/Electron）
4. legacy interpret/* 消费者迁移到读 L1+L2 表
5. markdown 截断阈值 30K → 60-80K

## 最近 10 commits

```
b8842a7 feat(frontend): PaperDetail 形态从 markdown 全文 → 速读卡片
c73a71d feat(electron+frontend): frameless 标题栏 + 自绘 header drag region
daaf73f fix(extract+api): docling _map_sections 累积 raw_text
60861c3 docs(pm): status snapshot 2026-04-27
d067321 fix(core): HF_HOME 优先复用用户全局缓存
ffc27a7 feat(frontend): SubscriptionPage Run now 异步反馈
7fc7075 feat(api+frontend): ft-027 Subscription 表单化 + Ingest
cd1c0d0 docs(pm): 关 iter-010 + 开 iter-011 ft-027 派发
ec90145 feat(frontend): ft-026 editorial/magazine 重设计 + CORS 修复
c50bf5f docs(pm): 开 iter-010 + dsp-009 ft-026 派发
```

## 下一步候选（按优先级）

1. **完成 ft-027 用户实测**（ingest 三阶段链路）→ 关 ft-027
2. **iter-012 / ft-028 启动**：Paper-centric schema 重构（**今日已派发 backend
   subagent**）
3. iter-013/014/015：Reading Station / Library / 通知（按序）
4. iter-016：ft-025 自动更新（产品形态稳定后再卷打包）
